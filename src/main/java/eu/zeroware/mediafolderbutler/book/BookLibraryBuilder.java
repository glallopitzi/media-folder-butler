package eu.zeroware.mediafolderbutler.book;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

public class BookLibraryBuilder extends BookLibraryWorker {
	
	private static Logger logger = LogManager.getLogger(BookLibraryWorker.class);
	
	public void build(){
		logger.debug("BookLibraryWorker.build()");
		// TODO
	}
}
